# Plan Review — Issue #18, Round 2

## Status
pass

## Summary
The plan and spec are tightly aligned and ready for implementation. Identifier/type fidelity is verbatim; every spec behaviour (including the new "`stop()` preserves `status.error`" clause at spec line 135, the `starting → stopped` mid-flight transition, and the strengthened reuse-after-stop check) is implemented by a dedicated, traceable test; running test counts close at 8 + 16 + 3 = 27; the dynamic-import-aware regex in Task 9 holds against every realistic import shape. No remaining risks.

## Findings

No findings.

## What looks good

- **Spec-vs-plan coverage is complete.** Every clause walks back to a task: the six-member `RunnerState` union (spec lines 65-71) → Task 1 Step 3 + Task 1 Step 1's `allStates` pin; `RunnerStatus` field shape (spec lines 73-79) → Task 1 types + Task 2 implementation; `SpawnSpec` (spec lines 81-86) → Task 1; `LogSnapshot` field names including `truncated` (spec lines 88-93) → Task 1 + Task 2's `buildCombined` helper; `LogOptions.sinceByteOffset` (spec lines 95-97) → Task 1's optionality test; `RunnerErrorCode` union (spec lines 107-111) → Task 1's per-code test; method contracts (spec lines 123-147) → Tasks 3-6 with the exact pre/post-conditions and transitions; `ScriptedRunnerScript` knobs (spec lines 165-171) → Tasks 4, 5, 6; engine-isolation constraint (spec lines 200-212) → Task 9 with the rg-verified premise that `src/workflow/` contains zero `tmux` substrings and zero `/runners` imports today.
- **`stop()` preserving `status.error` is correctly co-stated.** Spec line 135 ("When the prior state was `error`, `stop()` preserves `status.error` so the cause remains observable on the final `stopped` snapshot") is paired with the explicit assertion `expect(status.error).toBe('boom')` after `error → stopped` in Task 6 Step 1's "transitions error → stopped" test (plan lines 716-729). The Task 6 stop() implementation never touches `this.errorMessage`, so the assertion holds. Both halves agree.
- **The `starting → stopped` test is sound including the race guard.** Task 6's `spawn` amendment (`if (this.state === 'starting') { this.state = 'running'; ... }`) prevents the in-flight spawn from clobbering state after stop completes. Trace: spawn enters, sets state='starting', awaits `setTimeout(50)`; control returns to test; `status()` observes 'starting'; `stop()` runs synchronously to 'stopped'; setTimeout fires, spawn resumes, state is 'stopped' so the guard short-circuits; `spawnPromise` resolves; final `status()` reports 'stopped'. The test's `await spawnPromise.catch(() => {})` is defensive but harmless since the resumed spawn body returns normally. Final assertion `finalStatus.state === 'stopped'` holds.
- **Task 7's reuse-after-stop check is now strictly monotonic.** The 2ms `setTimeout` between the two spawns plus `expect(status.startedAt!.getTime()).toBeGreaterThan(firstStartedAt!.getTime())` would catch an implementation that forgets to overwrite `startedAt` on respawn. The non-null assertions are justified by the prior `toBeInstanceOf(Date)` assertions and are fine under strict mode (no `noUnusedLocals`/`noUnusedParameters`/`exactOptionalPropertyTypes` to bite). Task 7 is correctly framed as test-only/no-red-step in both its heading ("test-only, no red step") and the intro paragraph.
- **Task 8's barrel test is correctly typed.** `import type { Runner as RunnerType }` is a pure type import (NodeNext + ESM safe), and `* as runnersBarrel` gives value-level access to `RunnerError` and `ScriptedRunner`. The Task 8 Step 3 barrel re-exports both value-level (`RunnerError`, `ScriptedRunner`) and type-level (`Runner`, `RunnerErrorCode`, etc.) symbols matching the test's expectations. The deliberate `* as` import convention shift is acknowledged in the task intro.
- **Task 9's regex is robust on realistic inputs.** Traced against the requested samples: `from '../runners/types.js'` ✓ matches; `from '../../runners/index.js'` ✓ matches; `await import('../runners/index.js')` ✓ matches (the `\(?` and `\s*` allow the `(` and the leading whitespace inside dynamic-import calls); `from '../runner.js'` ✓ does NOT match (no `s` in `runner` and no path component named `runners`); a hypothetical `from './runners-helper.js'` ✓ does NOT match (the optional `(?:\/[^'"]*)?` group cannot consume `-helper.js`, and the trailing `['"]` then fails to match `-`). Verified with `grep -ri tmux src/workflow/` (zero hits) and `grep -r "runners" src/workflow/` (zero hits), so Task 9 will pass vacuously on first run as intended.
- **Internal test-count arithmetic closes cleanly.** Walking Step 4 of every task: Task 1 ends at 6 (4 in `Runner (structural)` + 2 in `RunnerError`); Task 2 +2 → 2 in `scripted-runner.test.ts`; Task 3 +4 → 6; Task 4 +1 → 7; Task 5 +1 → 8; Task 6 +6 → 14; Task 7 +2 → 16; Task 8 +2 → 8 in `runner-types.test.ts`; Task 9 = 3. Final file totals: `runner-types.test.ts` 8, `scripted-runner.test.ts` 16, `runner-engine-isolation.test.ts` 3. Sum 27. Matches the plan's Task 10 statement.
- **Convention adherence.** Tests are flat under `tests/unit/<name>.test.ts` (matching existing files like `agent-adapter-types.test.ts`, `scripted-agent-adapter.test.ts`); imports use `.js` extensions; vitest API (`describe`, `expect`, `it`) matches the rest of the suite; the new `src/runners/{types,scripted,index}.ts` layout mirrors `src/agents/{types,scripted,index}.ts` exactly.
- **TypeScript strict-mode compatibility holds.** `tsconfig.json` has `strict: true`, `module: NodeNext`, `target: ES2022`, `noEmitOnError: true`; no `noUnusedParameters` or `exactOptionalPropertyTypes`. The `_spec`/`_options` underscore-prefixed params are fine (the existing `src/agents/scripted.ts` already uses `_input`). Optional-field handling via the conditional snapshot builder in `status()` is correct without `exactOptionalPropertyTypes`. The Task 7 non-null assertions on `startedAt!` are permitted under strict.
- **TDD discipline is preserved.** Tasks 1, 2, 3, 4, 5, 6, 8, 9 each have a real failing-test step with a realistic expected failure mode (missing module, wrong state, missing knob, missing barrel). Task 7 is explicitly reframed as a coverage-pin commit with no red step, matching the framing in its heading and intro. Each task ends with a single commit carrying the required `Co-Authored-By` trailer via HEREDOC.
- **No placeholders.** Scan of the plan for `TBD`, `TODO`, `implement later`, `fill in details`, `similar to Task` returned zero matches. Every step lists concrete commands, file paths, and expected outcomes.
- **Round 1 fixes all landed.** Spec gained the `stop() preserves status.error` clause (line 135); Task 6 added the `starting → stopped` test and the spawn-race guard; Task 6's `error → stopped` test asserts `status.error` preservation; Task 7's reuse test now asserts monotonic `startedAt`; Task 7 reframed as no-red-step coverage-pin in both heading and intro; Task 8 second test switched to `import type { Runner as RunnerType }`; Task 9 regex extended to match dynamic `import(...)`; Task 7 commit step notes "only the test file is staged here, no source file changed".

STATUS=pass
