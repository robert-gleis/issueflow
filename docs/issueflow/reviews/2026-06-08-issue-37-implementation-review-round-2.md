# Implementation Review — Issue #37 Automatic Issue Decomposition — Round 2

**Spec:** `docs/issueflow/specs/2026-06-08-issue-37-design.md`
**Plan:** `docs/issueflow/plans/2026-06-08-issue-37-plan.md`
**Prior review:** `docs/issueflow/reviews/2026-06-08-issue-37-implementation-review-round-1.md`
**Branch:** `issue/37-automatic-issue-decomposition`

## Verdict

**pass**

All round 1 actionable findings are resolved. Build succeeds and all 46 decomposition-related unit tests pass. No new bugs, spec violations, or blocking gaps identified.

---

## Round 1 Fix Verification

| Round 1 finding | Status | Evidence |
|-----------------|--------|----------|
| Idempotent approve skips `parent_issue` consistency check | **Fixed** | `decomposition.ts:316-320` throws `DecompositionValidationError` when `applied.parent_issue !== issueNumber` before printing children |
| Approve parent mismatch (preview) untested | **Fixed** | `decomposition-command.test.ts:371-384` — exits `1`, `createChildIssues` not called |
| Approve parent mismatch (applied record) untested | **Fixed** | `decomposition-command.test.ts:386-400` — exits `1`, `createChildIssues` not called |
| `ChildIssueCreationError` propagation untested | **Fixed** | `decomposition-command.test.ts:402-416` — exits `1`, applied record not written |
| Wrong parent in child body untested at approve level | **Fixed** | `decomposition-command.test.ts:418-433` — uses real `createChildIssues`, exits `1`, stderr contains `references parent #99` |

---

## Verification Commands Run

| Command | Result |
|---------|--------|
| `npm test -- tests/unit/decomposition-store.test.ts tests/unit/decomposition-runner.test.ts tests/unit/github-issues.test.ts tests/unit/decomposition-command.test.ts tests/unit/cli.test.ts` | **PASS** — 5 files, 46 tests (+4 since round 1) |
| `npm run build` | **PASS** — `tsc -p tsconfig.json` succeeds |

---

## Acceptance Criteria

| Criterion | Status | Evidence |
|-----------|--------|----------|
| Large issues broken into smaller child issues | **Met** | `decompositionPlanSchema` requires `children[]` min 1; default agent returns two children |
| Generated by LLM planner via `AgentAdapter` | **Met** | `runIssueDecomposer` → `decomposeIssue` with injectable `AgentAdapter` |
| Output schema structured and validated | **Met** | `validateDecompositionFile` on every read and edit save |
| Children NOT created until approval | **Met** | `createChildIssues` invoked only in `approve` (`decomposition.ts:328-332`) |
| Parent/child links in tracker | **Met** | `ensureParentSection` prepends or validates `## Parent\n\n#N` before each `gh issue create` |
| Inspectable/overridable CLI | **Met** | Four subcommands registered; edit uses temp file + `$EDITOR` with validation on save |

---

## Spec Alignment (Non-Acceptance Items)

| Item | Status | Notes |
|------|--------|-------|
| Preview at `<.git>/issueflow/decomposition.json` | Met | Path normalisation in `getDecompositionPath` |
| Applied record at `decomposition-applied.json` | Met | Read/write mirror preview store |
| `ISSUEFLOW_ENGINE=1` gate on generate/approve | Met | `requireEngineGate` returns exit `3`; tested |
| Autonomous auto-approve (`ISSUEFLOW_AUTONOMOUS=1`) | Met | No extra confirmation; tested |
| `--force` regeneration when not applied | Met | Applied record blocks even with `--force`; tested |
| Preview-exists guard without `--force` | Met | Tested |
| Idempotent approve with parent consistency | Met | **Round 2 fix** — applied-record path now validates `parent_issue` |
| Applied record only after all children succeed | Met | `writeDecompositionApplied` after `createChildIssues` returns |
| No workflow state machine changes | Met | No state-machine imports |
| Non-goals respected | Met | No heuristic gate, engine tick, nested decomposition, parent relabelling, event log |

---

## Error Handling and Exit Codes

| Spec error / condition | Implementation | Tested |
|------------------------|----------------|--------|
| `DecompositionNotFoundError` | Thrown from store; surfaced via handler | show, edit |
| `DecompositionValidationError` | Schema/store/parent mismatch | edit, approve (preview + applied parent mismatch) |
| `DecompositionAlreadyAppliedError` | Generate when applied record exists | generate (with and without `--force`) |
| `ChildIssueCreationError` | gh failure or wrong parent in body | `github-issues.test.ts`, approve integration test |
| `PlannerError` (parent mismatch) | Thrown in `runIssueDecomposer` | runner test |
| Engine gate | `ISSUEFLOW_ENGINE !== '1'` → exit `3` | generate, approve |
| Issue resolution failure | `IssueIdError` → exit `2` | show |
| Preview exists without `--force` | stderr hint → exit `1` | generate |
| Editor non-zero exit | stderr + exit `1` | **Not tested** (unchanged from round 1) |

---

## Findings

No new findings. Round 1 actionable suggestions are resolved.

### Round 1 Deferred Items (Unchanged, Accepted for v1)

These were noted in round 1 as non-blocking and remain intentionally deferred:

- **`readDecompositionApplied` has no structure validation** (`decomposition-store.ts:98-110`) — spec defers applied-record schema validation for v1
- **Minor CLI test gaps** — `generate` on `PlannerError`, `edit` on editor non-zero exit, optional `show` parent check
- **`ensureParentSection` regex** (`issues.ts:36`) — multiline `^` matches parent heading anywhere in body; unlikely false positives

---

## Implementation Review by Module

### `src/planner/decomposition-store.ts`

- Read/write/validate paths are consistent with team-plan `store.ts` patterns
- `assertParentIssueMatches` centralises parent consistency checks used by edit and approve
- Applied record typed but not schema-validated on read (v1 decision)

### `src/planner/decomposition-runner.ts`

- Mirrors `runTeamPlanner`: decompose → parent assertion → write preview
- `createDefaultDecompositionAgent` returns deterministic two-child plan with correct parent links in bodies

### `src/planner/index.ts`

- Barrel re-exports store, runner, and error types for engine/runner reuse

### `src/github/issues.ts`

- Sequential child creation with injectable `GhRunner`
- `ensureParentSection` called before gh invocation; validation errors propagate without re-wrapping
- `ChildIssueCreationError` carries `childIndex` and optional `stderr`

### `src/commands/decomposition.ts`

- Injectable `DecompositionCommandDeps` harness mirrors `plan.ts` conventions
- Approve idempotent path now validates `applied.parent_issue` before printing children
- Fresh approve path: read → `assertParentIssueMatches` → `createChildIssues` → `writeDecompositionApplied`
- Generate guards layered: applied-record rejection unconditional; preview-exists check bypassed only by `--force` when not applied

### `src/cli.ts`

- `registerDecompositionCommands(program)` registered alongside plan commands

### Tests

| File | Tests | Coverage highlights |
|------|-------|---------------------|
| `decomposition-store.test.ts` | 6 | read/write, validation, parent match, applied record |
| `decomposition-runner.test.ts` | 2 | happy path, planner parent mismatch |
| `github-issues.test.ts` | 5 | `ensureParentSection`, sequential create, gh failure |
| `decomposition-command.test.ts` | 22 | all four subcommands, engine gate, idempotency, **4 new approve error paths** |
| `cli.test.ts` | 1 | decomposition group registration |

---

## What Looks Good

- **Round 1 fixes are precise.** Parent check added only to the idempotent short-circuit; error message distinguishes applied vs preview mismatch (`applied decomposition parent_issue …` vs `decomposition parent_issue …`).
- **Approve error-path tests use real store I/O.** Wrong-parent-body test wires real `createChildIssues`, closing the integration loop from CLI through `ensureParentSection`.
- **No scope creep.** Changes limited to the idempotent guard and test additions; no unrelated refactors.
- **Plan fidelity maintained.** File layout, handler steps, and error types unchanged from round 1 baseline.

---

## Summary

Round 2 confirms the issue #37 v1 implementation is complete and merge-ready. The two round 1 suggestions (idempotent approve parent check, approve error-path tests) are fully addressed with code and tests. Build passes; 46 targeted tests pass. Remaining deferred v1 items from round 1 are documented and do not block shipping.

**pass** — ready to merge.
